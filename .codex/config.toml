# Project-local Codex defaults for this Unity workspace.
# Keep approvals interactive and writes scoped to the opened project.
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
writable_roots = ["."]
network_access = false
